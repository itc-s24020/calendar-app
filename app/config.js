// アプリ全体の設定をまとめるファイル。
// 値をここに集約しておくと、UI本体のロジックを触らずに表示条件を変えられる。

const CalendarConfig = {
  // 現在の月から前後何ヶ月分まで表示するか
  rangeInMonths: 12,

  // 曜日ラベル(日曜始まり)
  weekdayLabels: ["日", "月", "火", "水", "木", "金", "土"],

  // 1セルに表示する予定の最大件数。超過分は「他n件」でまとめる
  maxSchedulePreview: 2,
};